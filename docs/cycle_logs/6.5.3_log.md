## Cycle 1 — 2026-05-22T02:15:00+09:00

### Intent
Expose remaining cooldown details from `TacticalSkillBase.cs` and implement UI elements (energy bar, skill slots, overcharge gauge, fullscreen flash overlay) with high-fidelity USS styles and clean C# controller bindings in `GameHUD` for the tactical skills and item collection feedback.

### Analysis
- The project runs on Unity 6 and uses the Unity UI Toolkit (UXML/USS) and the New Input System.
- We need to expose remaining cooldown from `TacticalSkillBase.cs`. We can add `GetRemainingCooldown()` and `LastUseTime`.
- We need to handle `EnergyChangedEvent` and update the energy bar. Since we cannot modify `EnergyManager.cs`, we will define `EnergyChangedEvent` inside `TacticalSkillBase.cs` (under `Potop.Client.Core.Events` namespace) and raise/poll it from `GameHUD`'s Update loop and when executing skills.
- OverchargeController uses `OverchargeData` which contains a private field `_overchargeData` with `MaxGauge`. We will retrieve it using C# reflection to compute percentages without modifying `OverchargeController.cs`.
- Visual effects need to look modern and premium: we will use neon cyan colors, glassmorphism, hover scale transitions, and fullscreen overlay opacity transitions.

### Plan
1. Add public property/methods to `TacticalSkillBase.cs` and declare `EnergyChangedEvent`.
2. Modify `GameHUD.uxml` to declare layout for:
   - Energy bar
   - Skills slots container (EMP, Orbital Strike, Shield)
   - Overcharge gauge container
   - Fullscreen flash overlay
3. Modify `GameHUD.uss` to define premium USS visual styles and transition animations.
4. Modify `GameHUD.cs` to bind input actions, search for component references, monitor overcharge/cooldown values, and hook up item collected events with temporary CSS classes and Coroutines.
5. Run the validation command via harness script.

### Failure Modes
1. **Reflection Failure**: If `_overchargeData` field name or type changes in `OverchargeController.cs`, reflection might return null, leading to `NullReferenceException`. *Mitigation: Handle nulls safely and fall back to 100f.*
2. **C# Formatting Guidelines**: Modifying C# classes with wrong brace style (ANSI vs. K&R) or access modifiers. *Mitigation: Meticulously use K&R style and explicit modifiers as defined in client `AGENTS.md`.*
