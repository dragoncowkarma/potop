## Cycle 1 — 2026-05-26T23:38:00+09:00

### Intent
Implement the TitanCore boss prefab, materials, animator controller, ShieldRingRotator script, and validation tests for Task 7.1.

### Analysis
- Target: `potop_client/Assets/Prefabs/Enemies/TitanCore.prefab` and corresponding materials/scripts/tests.
- Prefab structure must match: `Root (EnemyBase, Animator) -> Body (MeshRenderer) -> ShieldRing (ShieldRingRotator) -> LaserEmitter (Transform) -> HitboxCollider (Collider)`.
- Body scale must be 5x larger than normal enemies.
- We need 3 URP Lit materials: `Phase1.mat` (Blue), `Phase2.mat` (Purple), and `Phase3.mat` (Red) under `Assets/Materials/Boss/` with emissive intensity.
- Runtime material changes must prevent GC allocation (waived for now, or done via script/property blocks later, but we must make sure materials exist).
- Setup Animator Controller with states: `Idle`, `Phase1`, `Phase2`, `Phase3` and triggers.
- `ShieldRingRotator.cs` rotating at 120 RPM (720 deg/s) around Y-axis. Methods to decouple Y-axis shield from parent and trigger particle effects.
- EditMode tests in `TitanCorePrefabTests.cs` loading prefab and validating structure.

### Plan
1. Create `ShieldRingRotator.cs` in `potop_client/Assets/Scripts/Gameplay/VFX/`.
2. Create URP Lit materials for Phase 1, 2, and 3 under `potop_client/Assets/Materials/Boss/`.
3. Create animator controller `TitanCoreAnimator.controller` and the prefab `TitanCore.prefab`.
4. Create the EditMode test `TitanCorePrefabTests.cs` to assert prefab structure, scale, components, and active collider.
5. Run tests, verify they pass, and update task status/Kanban board.

### Failure Modes
1. Missing components in EditMode test if prefab structure has typos or incorrect structure.
2. Prefab saving formatting issue if manually created. We will write standard Unity YAML prefab formatting matching existing variant prefabs but tailored for TitanCore structure.
