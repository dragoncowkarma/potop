## Cycle 1 — 2024-05-18T10:00:00Z

### Intent
The goal is to implement EditMode tests for the Phase 6 systems: Overcharge, EnergyManager, TacticalSkill, ItemDrop, and MetaUpgrade, as specified in task 6.5.2. This is the RED phase, so the focus is purely on writing failing tests that define the behavioral expectations without modifying production code.

### Analysis
The current codebase lacks test coverage for the newly implemented Phase 6 systems. The target systems are:
- `OverchargeController`: Needs tests for gauge charging, decaying, overheating, and fever interaction.
- `EnergyManager`: Needs tests for adding, consuming, clamping, and event emission.
- `TacticalSkillBase` (and derived `EMPSkill`, `OrbitalStrikeSkill`): Needs tests for execute conditions (energy, cooldown) and specific behavior mocking.
- `ItemSpawner`: Needs tests for guaranteed drops, probability logic, null pool manager, and empty drop table.
- `MetaUpgradeManager`: Needs tests for level caps, gem costs, purchasing, and stat bundle generation.

The constraints are strictly RED phase: I must only create files in `Assets/Scripts/Tests/EditMode/` and not modify production code.

### Plan
1. Create `OverchargeTests.cs` to test the state machine and gauge logic of `OverchargeController`.
2. Create `EnergyManagerTests.cs` to test the single-point energy logic and events.
3. Create `TacticalSkillTests.cs` to test base execution conditions and specific skill behaviors via mocks/simulations.
4. Create `ItemSpawnerTests.cs` to test drop logic and edge cases.
5. Create `MetaUpgradeTests.cs` to test upgrade purchases and stat calculation.
6. Verify all files.

### Failure Modes
1. The tests might rely on `GameManager` or `EventBroker` in a way that requires proper tearing down or mocking, causing inter-test pollution.
2. The tests might fail to compile if namespaces or internal classes are not correctly referenced or are inaccessible.
